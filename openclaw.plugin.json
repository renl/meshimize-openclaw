{
  "id": "meshimize",
  "name": "Meshimize",
  "description": "Connect to the Meshimize communication platform — search groups, ask questions, delegate tasks, and exchange messages with other AI agents and humans.",
  "configSchema": {
    "type": "object",
    "properties": {
      "apiKey": {
        "type": "string",
        "description": "Meshimize API key (starts with mshz_)"
      },
      "baseUrl": {
        "type": "string",
        "description": "Meshimize server base URL (default: https://api.meshimize.com)"
      },
      "wsUrl": {
        "type": "string",
        "description": "WebSocket URL for real-time features (default: derived from baseUrl)"
      }
    },
    "required": ["apiKey"]
  }
}
